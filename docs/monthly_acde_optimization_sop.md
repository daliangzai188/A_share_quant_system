# ACDE月度数据更新、策略优化与精确回测标准流程

最后更新：2026-09-01
状态：当前有效的收益高优先研究与验收标准；正式发布仍须P11后独立认证

## 一、唯一目标

每个自然月结束后，系统使用截至上月末的最近3年完整数据，在已登记的可量化候选和
真实交易约束下，为紧接着的下一个自然月选择组合复利最高且可复现的ACDE版本。

```text
每月用最新3年数据优化，预测下个月
```

“本月最佳”严格定义为：

> 在本轮所有已经登记并实际测试的候选中，先排除因子不可量化、数据不合格、回测
> 失败或不可复现、研究与回测及执行逻辑不一致的方案，再选择最近3年真实
> `action_date`单账户回放中`A>C>E>D`组合期末复利倍数最高的方案。

它不表示已经找到任意因子组合中的理论最高值，也不承诺下个月收益最高。候选集合
必须完整留档；新增因子或扩大搜索必须开始新的有编号运行并记录全部测试，不得只保留
赢家。样本数、回撤、收益集中、压力测试和过拟合属于强制风险披露，不再作为收益排名
前置否决条件。

## 二、用户指令与授权边界

### 只更新数据

```text
更新最近三年策略数据
```

只执行数据更新、严格as-of底座重建和质量门禁，不运行参数搜索，不修改正式策略。

### 更新数据并优化全部策略

```text
更新最近三年策略数据并优化ACED
```

依次执行本文全部研究流程，输出建议版本；未经用户明确批准，不自动写入生产配置、
不提交实盘代码、不打开BUY。

### 只优化某一条腿

```text
更新最近三年策略数据并优化策略C
```

只允许替换C；A、E、D及组合腿序、仓位、成本、成交和退出规则全部冻结。A、E、D
同理。不得借优化一条腿顺带改变其他腿。

## 三、每月时间窗口

日期按A股市场时区`Asia/Shanghai`计算。设请求月份的上一个自然月末为`T`：

```text
研究窗口：T所在月份向前推35个月的月初 ～ T
评价日期：start_date <= action_date <= T
预测月份：T之后紧接着的一个完整自然月
```

例如2026年9月执行月度研究：

```text
研究数据：2023-09-01～2026-08-31
预测执行：2026-09-01～2026-09-30
```

若`T`为休市日，数据实际末日是不晚于`T`的最后一个交易日，但自然月截止日仍记录为
`T`。买入或退出结果在`T`时尚不可见的交易必须标记`OUTCOME_NOT_OBSERVABLE_AT_UPDATE`
或`PENDING_EXIT`并排除本轮收益评价，禁止读取`T`之后的价格。

## 四、流程总览

```text
0. 自动计算三年窗口和预测月份
    ↓
1. 更新原始数据并完成数据质量门禁
    ↓
2. 冻结数据、代码、配置和旧策略基线
    ↓
3. 精确复现当前正式版本；不一致则停止
    ↓
4. 登记可量化候选、搜索范围、代码版本和唯一收益排序标准
    ↓
5. A/C/E/D逐腿独立回测，其他三腿冻结
    ↓
6. 执行可量化、数据、回测成功和逻辑对齐四项硬条件
    ↓
7. 枚举全部通过硬条件的候选组合并按A>C>E>D完整回放
    ↓
8. 按统一排序规则选择最近三年组合复利最高者
    ↓
9. 生成新旧策略、同窗口收益、样本和规则差异报告
    ↓
10. 停止并等待用户审核；本阶段禁止修改正式配置或提交代码
    ↓
11. 用户明确要求再次精准校验后，使用独立认证入口逐笔重算并对账
    ↓
12. 校验全部成功才修改正式配置、回归并提交代码
    ↓
13. 显式冻结和发布后，才用于预测下个月
```

数据、基线复现、回测执行或逻辑对齐失败时，整轮必须停止或排除受影响候选。样本少、
回撤高、收益集中、压力测试变差或过拟合风险高时不得隐藏，必须生成风险标签和复核报告，
但不能据此跳过组合复利第一名、改选收益较低方案。

### 不可跳步的流水线签核表

每轮必须生成唯一`run_id=<cutoff_date>_<启动时间>_<Git短哈希>`，并严格按下表流转：

| 关卡 | 名称 | 必要输入 | 必须产物 | 失败结果/放行人 |
|---|---|---|---|---|
| `P00` | 请求与日期冻结 | 用户指令、系统日期 | `date_window.json`、授权范围 | 日期不明即停止 |
| `P01` | 原始数据更新 | P00窗口、数据源 | 增量下载日志、原始覆盖表 | 缺失即`NOT_READY` |
| `P02` | 数据质量门禁 | P01原始数据 | `quality_summary.json`、缺失清单 | 任一硬门失败即停止 |
| `P03` | 输入冻结 | 通过P02的数据 | `data_manifest.json`、代码/配置哈希 | 哈希不全即停止 |
| `P04` | 旧策略复现 | P03冻结输入、正式版本 | `baseline_reproduction.json`、逐笔旧账本 | 不一致即停止 |
| `P05` | 候选登记 | P04基线、量化因子研究空间 | `candidate_manifest.json` | 新增候选须另开运行并完整留档 |
| `P06` | 逐腿精确回测 | P05候选、冻结其他三腿 | 全候选逐笔账本与指标 | 任一口径漂移即停止 |
| `P07` | 逻辑对齐与策略专项回归 | P06计划与账本 | `c_regression_gate.json`等 | 逻辑不一致即排除该候选 |
| `P08` | 全组合回放与唯一排名 | 通过四项硬条件的全部候选 | `bundle_comparison.csv`、收益第一名 | 无可回测候选即`NOT_READY` |
| `P09` | 新旧三组对比 | OLD/NEW规则与两个月度窗口 | 规则、交易、收益归因文件 | 发现数据或逻辑错误即停止 |
| `P10` | 风险与极端变化复核 | P09对比结果 | `anomaly_review.json` | 风险披露；发现四项硬条件失败才退回 |
| `P11` | 用户审核停点 | 完整对比报告 | 用户明确选择或保持当前 | 只有用户可放行 |
| `P12` | 再次独立精准校验 | 用户选定的唯一候选 | 独立回放、测试、正式认证 | 任一失败即不得提交 |
| `P13` | 配置落地与代码提交 | P12全部通过、用户提交授权 | 发布冻结、Git提交哈希 | 未授权不得执行 |
| `P14` | 下个月真实前向账本 | 已发布版本、真实执行数据 | 候选/委托/成交/收益月账本 | 不反向修改历史结论 |

流水线控制规则：

1. 每个关卡只能读取上一关卡已经冻结的产物和哈希，不能跨级读取临时文件。
2. 每个关卡必须写出`PASS/FAIL`、开始结束时间、输入哈希、输出哈希和失败原因。
3. 上游数据、代码、配置、候选清单或费用规则发生任何变化，所有下游`PASS`立即失效，
   必须从受影响的最早关卡重新执行，禁止只重跑最后认证。
4. `INSUFFICIENT_SAMPLE`是必须展示的高风险标签，不是候选淘汰条件，也不改变收益排名。
5. 生成的逐笔账本和报告禁止人工直接改数；修复必须回到生成代码并重新运行。
6. 优化器和独立认证器不得共享内存中的交易表或汇总值；认证器必须从冻结文件重新读取。
7. P08选出的胜者在P09之后不得因风险偏好变化而改变；只有查明四项硬条件失败时，
   才退回相应关卡排除该候选并重新全量排名。
8. P11之前不得修改正式配置或提交；P11之后也只有用户明确授权的候选可以进入P12。
9. P12必须连续独立运行两次并得到相同的逐笔计划签名、指标和胜者ID。
10. 每轮给用户的报告首页必须展示P00～P14状态矩阵，不能只展示最终复利。

## 五、第一道门：数据更新标准

数据更新必须完整执行
[ACDE月度最近三年数据更新规范](monthly_three_year_data_update.md)，并取得：

```text
READY_FOR_MONTHLY_ACDE_RESEARCH
```

以下任一情况必须停止优化：

- 交易日、日线、`daily_basic`、涨停池、涨跌停价或复权数据缺失；
- A/C/E使用的日频特征未更新到窗口末日；
- D一分钟路径、触板母池或回封事件覆盖未达到冻结要求；
- 数据中存在未解释的重复、日期错位、未来可见字段或供应商异常；
- 派生表、输入清单和原始数据日期不一致；
- 任何研究输入晚于自然月截止日。

严格as-of成交模型必须从冻结的2019-01-01历史起点逐日恢复，但所有候选选择与指标
仍只允许按最近36个完整自然月的真实`action_date`计算。P09必须在旧版和新版的共同
历史区间核对成交模型字段与A/C/E/D基线计划；存在未解释漂移时不得进入胜者审核。

## 六、第二道门：基线冻结与精确复现

每轮优化开始前必须冻结：

1. 当前生产发布编号、A/C/E/D规则和配置；
2. `A>C>E>D`固定顺序；
3. 82.5%目标总仓位、85%单票硬顶和最大一只持仓；
4. 信号、计划、买入、退出、资金占用和成交规则；
5. 手续费、印花税、过户费、最低佣金和滑点；
6. 数据文件、代码、配置、交易日历及其SHA256；
7. 本轮开始时的Git提交和工作区状态。

优化器运行前必须先在新三年窗口复现当前正式版本，并输出逐笔基线账本。以下项目必须
逐项一致：

- 候选股票、信号日、`action_date`、买入日和实际退出日；
- 策略腿、优先级、计划状态、成交状态和被阻挡原因；
- 买卖价格、数量、费用、滑点和单笔账户收益；
- A/C/E/D分腿交易数、组合交易数和资金占用区间；
- 期末权益、复利倍数、最大回撤和连续亏损。

旧版本在相同输入下无法精确复现时，状态必须为`BASELINE_REPRODUCTION_FAILED`，禁止
开始候选搜索。不得拿旧报告中的倍数与新窗口结果直接比较。

## 七、第三道门：候选登记与搜索留痕

每轮回测前必须生成`candidate_manifest.json`；运行中新增候选时必须生成新的运行编号和
清单版本，至少记录：

- 被优化策略腿；
- 当前规则`KEEP_CURRENT`候选；
- 每个候选的因子、方向、阈值、排序、过滤和退出规则；
- 候选总数、去重后的计划签名数和最大搜索次数；
- 每个候选改变的轴数；
- 四项真实性硬条件、收益排序键和并列决胜规则；
- 生成时间、代码/配置哈希和研究截止日。

固定原则：

1. 当前正式规则必须作为候选之一，不能强迫每月换策略。
2. 单因子、多因子、阈值、排序、过滤和退出候选都可以搜索，但每条规则必须能由信号
   时点可见数据机械计算，并记录实际测试次数。
3. 相同计划签名的候选只能保留一个，禁止用参数名称制造虚假搜索数量。
4. 看完收益后新增因子、缩小亏损区间、改变阈值或扩大搜索，必须另开运行、完整重跑
   并标记`STRICT_DISCOVERY`；该标签披露过拟合风险，但不自动取消其收益冠军或落地资格。
5. `STRICT_DISCOVERY`候选仍须通过数据、回测和逻辑对齐检查；已经看过的三年数据不能
   冒充独立样本外，下个月真实前向结果仍须单独封存并展示。
6. 所有被测试过的候选，包括失败和人工放弃项，都必须写入候选账本，禁止只报告赢家。

## 八、第四道门：ACDE真实交易回测口径

### 1. 固定顺序和计划时点

组合顺序永久固定：

```text
A > C > E > D
```

- A/C/E只使用上一交易日收盘后已经冻结的静态计划，在T+1执行；
- D只有A/C/E当日都不存在静态计划时才能启动盘中路径；
- 已存在的高顺位静态计划即使最终无法成交，也继续阻挡低顺位，不允许事后用低顺位
  补回当天收益；
- 禁止按信号日同日收益重新排序，统一按真实`action_date`裁决资金。

### 2. 各腿持有期

- A：T日收盘选股，T+1开盘买入，T+2收盘计划卖出；
- C：T日收盘选股，T+1开盘买入，T+3收盘计划卖出；
- E：T日收盘选股，T+1开盘买入，按当轮冻结的E退出规则卖出；
- D：信号日盘中真实路径成交，信号日后第2个交易日收盘计划卖出。

计划退出日停牌或跌停无法卖出时，必须顺延到第一个真实可卖日。退出日收盘前资金仍被
占用，不能同日提前用于下一笔交易。

### 3. A股成交规则

- T+1约束必须生效；当日买入不得当日卖出；
- 一字涨停、涨停排队、停牌或成交概率不足不得默认买入；
- 跌停、停牌或流动性不足不得默认卖出；
- 主板、ST、创业板、科创板和北交所使用各自当日有效涨跌停规则；
- 申报数量、价格笼子、涨跌停比例和无涨跌幅日期必须按交易日期版本化，禁止把当前
  交易规则倒灌到规则生效前的历史交易；
- D必须使用一分钟事件顺序和信号时点可见字段，禁止用收盘后的`first_time`、
  `last_time`或最终炸板结果回看盘中；
- 未知成交必须明确标记，不能按盈利方向乐观处理。

### 4. 价格、数量、费用和现金

- 信号收益与长期比较使用前复权口径；订单价格、涨跌停判断和现金结算使用当日真实
  未复权交易价格，禁止在同一环节混用；
- 买入执行价加入买入滑点，卖出执行价扣除卖出滑点；当前基准为双边各0.1%；
- 佣金双边各0.03%，单边最低5元；过户费双边各0.001%；
- 印花税只在卖出收取：2023-08-27及以前0.1%，2023-08-28起0.05%；
- 股数必须按股票所属市场和交易日当时有效规则计算：沪深主板及深市普通股票买入按
  100股或其整数倍；科创板限价申报最低200股、超过200股后可按1股递增；北交所
  竞价申报最低100股并按其当日有效增量规则处理。卖出零股按交易所规则一次性处理；
  禁止对所有板块统一套用100股整数手；
- 使用真实现金账本：买入扣除成交额和费用，卖出增加成交额并扣除费用；未投入现金
  保持为现金，不得对82.5%仓位简单假定无限可分；
- 除权、分红、送转和退市事件必须按统一复权/持仓调整口径处理并留痕。

### 5. 组合资金

- 初始认证资金为500,000元；
- 目标总仓位82.5%，单票硬顶85%，最大持仓数1；
- 持仓未实际退出前不得开下一笔；
- 同日多个策略计划只按`A>C>E>D`选择唯一资金赢家；
- 每笔收益进入账户后，下一笔按当时真实可用现金重新计算股数；
- 机械复利必须从逐笔现金账本得到，不得直接把不一致口径的收益率列相乘。

## 九、策略C强制回归清单

为防止再次发生C口径错误，每轮只要涉及C或组合回放，以下测试必须全部通过：

1. C候选由T日收盘可见数据产生，`action_date`严格为下一交易日；
2. C买入为T+1开盘规则，计划退出为T+3收盘规则，禁止误用A的T+2；
3. C的`risk_flags`必须在C过滤前用信号时点可见数据重新构建；
4. 同一`action_date`存在A计划时，C必须让路，不能按C收益更高事后改选；
5. C被高顺位计划阻挡、无法买入、无法卖出和顺延退出必须分别编码；
6. C持仓的退出日收盘前继续占资，不能与下一笔重复使用资金；
7. C独立回测和“只替换C后的ACED回测”必须分别生成逐笔账本，交易数不能混用；
8. C未改变时，新旧C计划签名必须完全相同；只替换C时，A/E/D计划签名必须完全相同；
9. C候选、组合实际采用的C和正式发布配置三者规则ID必须一致；
10. 独立认证器必须逐笔复核C的股票、日期、收益和让路原因，任何差异立即失败关闭。

测试结果必须写入`c_regression_gate.json`。任一项失败，整轮不得产生`UPDATE_C`。

## 十、可落地候选四项硬条件

候选只有以下四项是进入收益排名的硬条件：

1. **因子可量化**：入选、过滤、排序、退出和仓位均有明确字段、公式、阈值、缺失值
   处理和信号可见时点，不含主观选股、事后挑样本或不可执行标签。
2. **数据有效**：P02及严格as-of检查通过，无未来数据、重复主键、日期错位、复权错误、
   关键字段缺失或无法解释的数据漂移。
3. **回测成功且可复现**：A股成交规则、费用、滑点、现金占用和退出约束全部生效，
   回测完整结束；相同冻结输入连续运行得到相同计划签名、逐笔账本和指标。
4. **逻辑完全对齐**：因子研究、候选生成、单腿回测、ACED组合回放、模拟信号和拟发布
   配置使用同一规则；未获授权的腿序、其他策略腿、仓位和执行口径保持冻结。

以下项目全部必须计算和披露，但不是候选淘汰或收益改排条件：

- 单腿是否高于当前腿、只替换该腿是否提高组合；
- 交易样本数、样本保留率、胜率、平均收益和中位数收益；
- 最大回撤、最大单笔亏损、连续亏损、盈亏比和收益集中度；
- 不同月份、行情、滑点、费用和容量压力测试；
- `STRICT_DISCOVERY`、搜索次数、规则复杂度、参数敏感性和真实前向样本数。

这些诊断无论好坏都不能把组合复利第一名换成第二名。只有诊断进一步证明数据错误、
未来泄漏、回测未真正成功或研究/回测/执行逻辑不一致时，才按对应硬条件取消候选资格。

## 十一、唯一胜者选择规则

### 1. 单腿排名

对每条被优化策略：

1. 只排除未通过第十节四项硬条件的候选；
2. 对其余候选按“只替换该腿后的ACED组合3年期末复利倍数”从高到低排序；
3. 若复利在数值容差内相同，依次选择：组合最大回撤更小、单腿最大回撤更小、规则
   改动轴更少、样本数更多、`variant_id`字典序更小者；
4. 当前版本参与同一排名；它不是复利第一名时，不得因新候选样本少、回撤高、单腿
   收益低或过拟合风险高而强制`KEEP_CURRENT`。

单腿第一名用于报告和归因，不得作为多腿组合搜索的前置剪枝，因为两个非单腿第一名
候选在固定腿序下仍可能形成全局复利最高组合。

### 2. 多腿最终组合

必须枚举A、C、E、D全部通过四项硬条件的去重候选笛卡尔组合，至少包括当前ACED、
每条腿单独替换和所有多腿替换。每种组合都必须按真实`A>C>E>D`重新回放，不能把各腿
独立复利相乘，也不能只组合各腿单独排名第一的候选。样本或回撤不得用于组合搜索剪枝。

最终唯一胜者按以下顺序确定：

1. 最近3年ACED组合期末复利倍数最高；
2. 并列时组合最大回撤更小；
3. 再并列时组合中位数收益更高；
4. 再并列时修改腿数更少、规则总改动轴更少；
5. 最后按稳定的方案ID字典序决定，保证重复运行结果一致。

这就是“新数据下最好”的唯一含义。风险诊断不能改变该机械排名；如用户主动选择
非第一名，必须记录为人工选择，不能把它标记为机械最优或收益最高。

## 十二、新旧策略与上月结果强制对比

选出研究胜者后，必须先完成对比并停止，不能直接修改正式配置或提交代码。

### 1. 三组必须重放的结果

每轮至少同时生成：

| 编号 | 策略规则 | 数据窗口 | 用途 |
|---|---|---|---|
| `OLD_ON_OLD` | 上个月正式策略 | 上个月的3年窗口 | 复现上月已报告结果 |
| `OLD_ON_NEW` | 上个月正式策略 | 本月最新3年窗口 | 分离数据滚动和数据修订影响 |
| `NEW_ON_NEW` | 本月研究胜者 | 本月最新3年窗口 | 与旧策略进行公平同窗口比较 |

策略选择只允许使用`OLD_ON_NEW`与`NEW_ON_NEW`的同窗口差异。`OLD_ON_OLD`只用于解释
为什么上月报告和本月报告发生变化，不得把不同窗口倍数直接当成策略增益。

使用对数复利分解：

```text
窗口/数据效应 = log(OLD_ON_NEW复利 / OLD_ON_OLD复利)
规则变化效应 = log(NEW_ON_NEW复利 / OLD_ON_NEW复利)
总变化       = log(NEW_ON_NEW复利 / OLD_ON_OLD复利)
```

还必须单独检查两个窗口重叠的35个月。如果旧策略在重叠期的计划或收益发生变化，必须
归因到数据供应商修订、数据修复、代码变化或费用规则变化；任何无法解释的变化都是
`UNEXPLAINED_BASELINE_DRIFT`并停止本轮。

### 2. 规则差异必须用中文逐项展示

每个被修改策略必须列出：

| 项目 | 上月正式策略 | 本月候选策略 | 操作 | 影响 |
|---|---|---|---|---|
| 因子 | 原因子 | 新因子 | 新增/删除/保持 | 改变多少候选和实际交易 |
| 阈值 | 原区间 | 新区间 | 放宽/收紧 | 新增或排除哪些样本 |
| 排序 | 原排序键 | 新排序键 | 替换/保持 | 每日第一名变化次数 |
| 过滤 | 原条件 | 新条件 | 新增/删除 | 被过滤交易及收益 |
| 退出 | 原持有期/退出条件 | 新规则 | 替换/保持 | 退出日变化及收益 |

不能只给`variant_id`或一句“复利提高”。必须让用户能够看到策略究竟改了什么、影响了
哪些交易、利润改善来自哪里。

### 3. 收益和样本对比表

`OLD_ON_OLD`、`OLD_ON_NEW`、`NEW_ON_NEW`必须并排报告：

- 组合和A/C/E/D分腿样本数；
- 胜率、平均收益、中位数收益；
- 期末权益、复利倍数和净利润；
- 最大回撤、最大单笔盈亏、盈亏比和最大连续亏损；
- 月度、季度、年度收益；
- 总成本、滑点、无法成交和延期退出；
- 新增交易、删除交易、同票改期交易和收益变化交易；
- 前1、前3和前5笔交易对复利改善的贡献。

### 4. “好太多或差太多”异常复核

以下阈值用于强制风险复核。命中任一项不等于候选错误，也不改变P08收益排名，但状态
必须附加`EXTREME_CHANGE_REVIEW_REQUIRED`，在解释和独立认证完成前禁止提交：

| 指标 | 默认复核阈值 |
|---|---|
| `NEW_ON_NEW / OLD_ON_NEW`复利倍数比 | 大于等于1.50 |
| `OLD_ON_NEW / OLD_ON_OLD`复利倍数比 | 小于等于0.67或大于等于1.50 |
| 组合最大回撤绝对变化 | 大于等于5个百分点 |
| 组合实际交易数变化 | 大于等于15% |
| 胜率绝对变化 | 大于等于8个百分点 |
| 平均收益或中位数收益绝对变化 | 大于等于1.5个百分点 |
| 最大单笔亏损绝对变化 | 大于等于3个百分点 |
| 规则增益由极少交易贡献 | 前3笔贡献超过规则总对数复利增益的50%，或全部增益由不超过3笔改变交易产生 |
| 重叠35个月旧策略计划变化 | 任一无法解释变化即失败；已解释变化达到5%仍需复核 |

异常复核至少回答：

1. 变化来自窗口滚动、原始数据修订、规则修改还是组合腿间让路？
2. 是否由C持有期、`action_date`、A让路、退出日占资或费用计算错误造成？
3. 是否只过滤了少数巨亏交易，或只保留了少数巨盈交易？
4. 是否出现未来数据、结果回看、幸存者偏差或成交过度乐观？
5. 去掉贡献最大的1笔、3笔、5笔后，新策略是否仍优于旧策略？
6. 将滑点提高到0.2%、0.5%、1%后，排序是否保持？

复核结论必须区分两类：

- 若发现数据错误、未来泄漏、回测失败、成交口径错误或研究与执行逻辑不一致，则该候选
  四项硬条件失败，退回对应关卡排除后重新全量排名；
- 若变化能够由已量化规则和逐笔账本解释，只是样本少、回撤高、收益集中、压力测试差
  或过拟合风险高，则保留收益冠军，并把风险标签完整提交用户审核。

### 5. 用户审核前必须停止

优化阶段最终只允许交付：

```text
新策略规则
旧策略规则
规则差异
新旧同窗口逐项收益对比
与上月报告的窗口/数据差异
样本和逐笔交易变化
异常复核结果
组合复利第一名及当前版本对比
```

交付后状态固定为`COMPARISON_PENDING_USER_REVIEW`。此时禁止：

- 修改正式策略配置；
- 修改实盘BUY开关；
- 把研究胜者写成已发布版本；
- 提交策略代码；
- 用研究结果替换当前生产基线。

## 十三、必须报告的统计

当前版本、每个候选、每条腿胜者和最终组合都必须报告：

- 样本数及A/C/E/D分腿交易数；
- 胜率、平均收益和中位数收益；
- 期末权益、总复利倍数和净利润；
- 最大回撤及起止日期；
- 盈亏比、最大单笔盈利和最大单笔亏损；
- 最大连续亏损次数及对应交易段；
- 月度、季度和年度收益分解；
- 佣金、印花税、过户费、滑点和总交易成本；
- 候选数、有效计划签名数、样本保留率和规则复杂度；
- 无法买入、无法卖出、停牌、跌停顺延和未知成交数量；
- 成交概率、资金容量和不同滑点压力测试；
- `STRICT_DISCOVERY`、过拟合风险和真实前向样本数。

不得只展示复利倍数，也不得隐藏失败候选、回撤、极端亏损或样本不足。

## 十四、用户批准后的再次精准校验与提交

只有用户明确表达以下含义后，才能进入本阶段：

```text
回测逻辑和数据再次精准校验，如果校验成功，提交代码
```

该授权只针对对比报告中已经明确标识的唯一候选版本，不得在最终校验阶段换候选、
改阈值或继续搜索。若候选发生任何变化，必须退回候选冻结和新旧对比阶段重新让用户
审核。

最终校验必须从磁盘重新加载冻结数据和配置，使用新的进程完整重跑，不得复用优化器
内存结果。顺序固定为：

```text
再次核对日期窗口、交易日历和数据截止日
    ↓
再次运行原始数据与严格as-of质量门禁
    ↓
核对数据、候选、代码和配置哈希未变化
    ↓
独立复现OLD_ON_NEW和用户选定的NEW_ON_NEW
    ↓
逐笔核对C专项回归、A>C>E>D让路和现金占用
    ↓
核对费用、股数、滑点、成交、权益、复利和回撤
    ↓
运行单元测试、集成测试和正式认证
    ↓
全部通过后才写入正式配置并生成发布冻结
    ↓
检查Git差异只包含本次授权修改
    ↓
提交代码并报告提交哈希
```

任一检查失败时不得提交，必须把失败项、影响和修复建议交给用户。禁止为了让认证通过
而修改预期收益、交易数、哈希或测试断言。

代码提交不等于向券商发送订单。是否启用新版本实盘以及何时启用，仍以用户明确授权、
发布生效日和执行风控为准；真实资金必须先小资金验证。

## 十五、独立认证与数值精度

用户完成新旧对比审核并授权最终校验后，必须使用独立认证入口从冻结输入重新生成计划
和逐笔现金账本，而不是直接复用优化器内存中的汇总结果。

认证要求：

1. 数据、代码、配置和候选清单哈希与本轮冻结清单完全一致；
2. 逐笔股票、策略腿、信号日、`action_date`、买卖日和状态必须完全一致；
3. 交易数、分腿数、让路数、未成交数和延期退出数必须整数完全一致；
4. 股数、成交金额和最低佣金按分精度核算；
5. 单笔收益、权益曲线、复利和回撤使用未四舍五入原值计算，只在展示时格式化；
6. 期末权益必须同时通过“逐笔现金流水求和”和“权益曲线最后值”两种计算核对；
7. 浮点比较容差固定为绝对误差`1e-10`，金额对账容差固定为0.01元；
8. 同一输入连续运行两次，报告、计划签名和胜者ID必须一致。

任何认证差异都必须标记`CERTIFICATION_FAILED`。不得通过修改预期交易数或预期复利
让测试变绿；只有查明数据、代码或口径原因并重新开始整轮流程后才能继续。

## 十六、月度输出目录

每轮结果保存到：

```text
reports/monthly_acde_research/<cutoff_date>/
```

至少包含：

```text
date_window.json
data_manifest.json
quality_summary.json
baseline_freeze.json
baseline_trades.csv
baseline_reproduction.json
candidate_manifest.json
all_candidate_metrics.csv
leg_winners.json
bundle_comparison.csv
rule_diff.json
trade_diff.csv
metric_attribution.json
anomaly_review.json
selected_trades.csv
c_regression_gate.json
backtest_reconciliation.json
certification.json
decision_report.md
artifact_manifest.json
```

## 十七、正式结论状态

每条腿和最终组合只能使用以下状态：

- `NOT_READY`：数据或必要输入不完整；
- `BASELINE_REPRODUCTION_FAILED`：当前版本无法精确复现；
- `STRICT_DISCOVERY`：结果回看或扩大搜索风险标签，不是终止状态或自动否决条件；
- `KEEP_CURRENT`：当前版本组合复利仍最高，或所有更高复利候选均未通过四项硬条件；
- `RETURN_FIRST_WINNER`：通过四项硬条件后的最近3年ACED组合复利第一名；
- `EXTREME_CHANGE_REVIEW_REQUIRED`：相对上月或旧策略变化异常，需要逐笔解释；
- `COMPARISON_PENDING_USER_REVIEW`：新旧对比已交付，禁止改正式配置和提交代码；
- `RESEARCH_WINNER_PENDING_CERTIFICATION`：用户已选定唯一候选，等待再次精准校验；
- `CERTIFICATION_FAILED`：独立回放不一致；
- `FINAL_VALIDATION_PASSED`：用户已授权且再次精准校验通过，可以执行本次授权的提交；
- `RELEASED_FOR_NEXT_MONTH`：用户明确批准并完成冻结、回归和显式发布。

`RETURN_FIRST_WINNER`和`STRICT_DISCOVERY`标签本身都不等于已经发布。收益第一名必须先
到P11供用户审核，再经P12独立重算与逻辑对齐认证，取得用户明确授权后才能写入正式配置。

## 十八、发布和下个月前向记录

只有用户审核完新旧对比并明确要求“再次精准校验，成功后提交代码”，且独立认证全部
通过后，才能：

1. 生成带截止日的发布编号；
2. 写入正式策略配置；
3. 运行全量回归和执行安全测试；
4. 提交代码与不可变认证产物；
5. 从下个月第一个交易日起生效。

下个月是真正未知的前向期。月中只更新数据、计算冻结规则信号和监控执行，不根据盈亏
改参数。下个月结束后先封存真实候选、计划、委托、成交、费用、滑点、收益和回撤，再
启动下一轮滚动3年研究。

实盘仍有亏损风险；任何新版本必须先模拟和小资金验证，历史最佳不代表未来盈利。

## 十九、交易规则依据

交易规则必须以交易所和税务机关在对应交易日有效的正式规则为准，并在每轮数据清单
中保存规则版本。当前核验入口包括：

- [上海证券交易所交易规则（2026年修订）](https://www.sse.com.cn/lawandrules/sselawsrules2025/stocks/exchange/c/c_20260424_10816482.shtml)
- [深圳证券交易所交易规则](https://www.szse.cn/lawrules/rule/repeal/rules/P020231230545338442079.pdf)
- [北京证券交易所交易规则](https://www.bse.cn/jygl_list/200028217.html)
- [财政部、税务总局关于减半征收证券交易印花税的公告](https://shanxi.chinatax.gov.cn/web/detail/sx-11400-545-1780448)

官网规则发生变化时，先更新日期化规则表和回归测试，再运行当月回测；不得只改文档
说明而继续使用旧计算逻辑。

## 二十、当前代码实现与剩余授权边界

截至2026-09-01，月度研究代码现状与新标准的差异为：

1. `config/acde_rolling_optimization.json`使用schema_version=2和每月36个完整自然月；
2. `scripts/optimize_acde_monthly.py`是正式月度研究入口，旧三窗口入口自动转交；
3. 现有标准执行器仍包含旧版单腿、样本和回撤硬门，尚未完全实现本文件规定的四项硬
   条件加全候选笛卡尔组合收益排序；在执行器改造并回归通过前，旧`eligible_winner`
   只能作为风险门禁诊断，不能覆盖全组合`RETURN_FIRST_WINNER`；
4. 回放使用500,000元真实现金、分市场申报股数、最低5元佣金、日期化印花税、过户费
   和双边滑点；
5. 输出P00～P14状态、候选清单、逐腿账本、组合子集、C回归、三组对比、异常复核和
   对账文件。

研究执行器不会自行越过P11。独立认证P12、正式配置落地和代码提交P13仍必须在用户看完
新旧对比并明确授权后单独执行；未认证研究胜者不得写成正式发布成功。2026-09-01用户
先授权V13，随后明确要求E弱收益区间直接空仓而不是降至25%仓位，并授权落地
`ACDE_E_NO_TRADE_10687_20260831_V14`。随后用户审核D扩样结果并授权落地
`ACDE_D_ACTIVE_LT20_OPEN2_12483_20260831_V15`。2026-09-02用户继续审核并授权落地
`ACDE_C_THIRD_BRANCH_T2_22695_20260902_V16`：A/E/D规则冻结，C保留原五个profile并
按T+3退出，新增第3逻辑分支并按T+2退出。正式配置、分支级退出、逐腿计划签名、
两次确定性精确现金回放和冻结逐日账本必须继续由
`scripts/certify_strategy_c_third_branch_release.py`对齐。代码提交仍是单独动作；该认证
不等于独立样本外或真实资金容量通过。

## 二十一、2026-09-02开仓口径缺陷处理

开仓条件、市场广度、仓位金额或成交概率在回测与实盘不一致，属于程序缺陷，
不是月中策略调参。处理顺序固定为：

1. 先修复生产候选选择和历史现金回放的共用口径；
2. 按同一真实`action_date`、`A>C>E>D`、资金占用和费用立即重算；
3. 历史必需字段缺失时fail-closed，禁止用事后价格结果伪造信号时可成交性；
4. 立即暂停不能证明回测与实盘一致的策略腿新开仓，但继续采集路径和证据；
5. 作废受影响的旧发布冻结，保留为历史对照，不得继续称为当前对齐结果。

本次D的信号时L2队列金额缺失按上述规则处理：D新BUY暂停，不受“收益优先”
规则豁免。收益优先只在因子可量化、回测数据无问题、回测成功且逻辑对齐之后生效。
