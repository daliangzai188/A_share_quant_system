# 策略 M 兜底补位腿

> 状态：**已实现并按用户明确风险接受恢复真实新开仓**
> （`strategy_m.enabled=true`、`live_order_enabled=true`）。M没有通过全部分段回撤
> 非劣门禁，认证状态必须是`PASS_WITH_RISK_ACCEPTANCE`，不能写成门禁已经通过。

> **2026-08-07 M 不再是"五腿全空才兜底"**：腿序重排为 D>L>A>M>E2>C 后，M 排在
> E2 和 C **之前**，只被 D持仓 / L信号 / A计划 挡住。配套改动：
> `config.json` 的 `strategy_m.require_all_legs_idle` 由 true 改为 false、
> `run_strategy_m_signal.other_leg_has_signal` 改名为 `higher_priority_leg_has_signal`
> 并只检查 L 与 A。本文下文凡出现"五腿全空/全部无候选才触发"的表述，均为该改动
> 之前的历史口径。M 自身的 10% 回撤保护不变，仍在信号生成侧生效。

## 一、它是什么

M 是第六条腿，只做一件事：**把原本完全空仓的日子利用起来**。

历史回放覆盖481个信号日。M只在账户空仓且D/L/A没有占用时判断，最终入选26笔。
因为M排在E2/C之前，它既补充空仓日，也可能优先于下游E2/C。

**它不改变其他腿的规则，但会按D>L>A>M>E2>C的固定腿序占用单账户资金。**

## 二、完整规则

```
触发前提（三条必须同时成立）：
  1. A daily ops 当日未生成A计划
  2. L 当日无正式信号
  3. 账户没有任何未平仓头寸   ← 这条同时覆盖 D

自有闸门（两条）：
  5. 当日 sz_main_market_sentiment_level = weak（深市主板情绪弱）
  6. 账户当前回撤 ≤ 10%

选股：
  当日涨停池 → 数据质量/可买性/非ST 过滤 → 取流通市值最小的一只

执行：
  T+1 开盘买入，T+2 收盘卖出，仓位 82.5%
```

第 3 条为什么能覆盖 D：D 在信号日**盘中**打板买入，成交后即出现在 `positions.json`。
M 在收盘后运行，看到持仓就退出，**因此 D 天然优先于 M，不需要额外逻辑**。

E2和C不在M的上游门中：两者排在M后面，不得反向阻断M。

## 三、优先级位置

```
①  D            信号日盘中打板，时序优先
②  L            通过model=3基础条件后无条件优先
③  A            收盘后计划
④  M            D/L/A均未占用且账户空仓时触发
⑤  E2           M无信号时补位
⑥  C            最后补位
```

## 四、回测结果

窗口 20240520~20260514，仓位 82.5%，口径同 `reports/current_portfolio_alignment`。

| 指标 | 不含M对照 | 当前实盘（含M） |
|---|---:|---:|
| 组合复利 | 7677.946823x | 29388.980134x |
| 最大回撤 | −23.51% | −23.56% |
| 胜率 | 70.37% | 68.67% |
| 组合笔数 | 135 | 150 |
| M自身入选 | — | 26 |

> **注（2026-08-07）**：以上为该改动**上线当时**的验收数据。此后 A/C 候选不再被
> `baseline.abc_return` 这张作废持仓表裁剪（commit f927d36），组合基准已更新为
> L扩容后 139笔/6907.348272倍、含M 155笔/20606.559742倍（**该组数字为当日中间态，
> 已被下方 2026-08-07 再次更新取代**）。
> 本处数字保留为历史验收记录，**不再是当前标尺**；当前标尺见
> `docs/strategy_benchmark_and_verdicts.md` 第一节。
>
> 2026-08-10更新：用户基于M自身26笔、平均账户收益+6.20%、中位数+2.09%和
> 单腿复利4.1639倍，明确决定恢复M。当前发布标尺为
> **150笔 / 29388.980134倍 / 最大回撤 -23.56% / 胜率68.67%**；不含M的
> 135笔/7677.946823倍保留为对照。两种倍数均为历史机械复利，不是收益预期，容量未认证。
M自身26笔账户收益：均值 **+6.20%**、中位数 **+2.09%**、胜率 **53.85%**，
单腿复利4.1639倍、最大单笔亏损−9.06%。它仍是典型右尾驱动分布。

### 当前上线判定

M提高全样本复利，但完整组合的“全部、训练半段、自然年2024”回撤变差，
没有通过“所有分段收益和回撤均非劣”门禁；规则又来自1053个方案搜索、样本外仅3笔。
用户已明确接受上述风险，因此当前`live_order_enabled=true`，认证使用独立状态
`PASS_WITH_RISK_ACCEPTANCE`。这不等于统计验证通过。500次随机对照无超越样本只能写
经验p值上界`1/501=0.001996`，不能写成p=0，也不能替代多重检验和前向验证。

## 五、证据强度（必须与收益数字一起读）

### 支持它的证据

| 检验 | 结果 |
|---|---|
| 随机选票对照（500 次） | 无一次超过真实值；plus-one经验p值上界 **1/501=0.001996** → "选市值最小"携带信息 |
| 随机选日对照（500 次） | plus-one经验p值上界 **1/501=0.001996**；随机选日只有4.8%能超过基线 → "weak"条件是必需的 |
| 触发条件单调性 | weak 3.11x > strong 0.44x > neutral 0.03x，方向合理 |
| 无条件在空档日交易 | **0.07x** —— 反证空档日整体有害，筛选不可省 |
| 分年 | 2024 / 2025 / 2026 **全部改善** |
| 分半年 | 5 段中 4 升 1 平 |
| 样本外 | 3 笔全正（+2.36% / +0.23% / +5.70%） |

### ⚠️ 反对它的证据（同等重要）

| 问题 | 具体表现 |
|---|---|
| **参数邻域塌陷** | 9 种排序里只有"流通市值最小"是好的（3.11 倍），**次优仅 1.13 倍**；T+2（3.11 倍）显著优于 T+3（1.39 倍）。**单点最优、邻域塌陷是过拟合的典型指纹。** |
| **样本内挑选** | 规则是从约 1053 个候选方案中按组合复利挑出的第一名 |
| **样本外极薄** | 只有 3 笔；实盘 2026-06~07 那两个月**一次都不会触发** |
| 触发稀疏 | 两年 25 笔 ≈ 每年 13 次，攒到 20 笔需 1.5~2 年 |
| 回撤恶化 | −18.84% → −24.68%，多 5.84 个百分点 |

**结论：规则方向有证据支持，但 3.37 倍这个倍数不可作为实盘预期。**
上线后应按"期望为正、倍数远低于回测"管理预期。

### 下行风险（已量化）

| M 的真实期望 | 组合复利 | vs 基线 |
|---|---:|---:|
| 回测水平 +5.50%/笔 | 15891x | 3.37x |
| 样本外水平 +2.76%/笔 | 6987x | 1.48x |
| **完全无效 0%/笔** | **3980x** | **0.84x（−15.5%）** |
| 负期望 −3%/笔 | 2127x | 0.45x |

**下行成本是固定的 −15.5%**，来自 M 顶掉的 11 笔原有交易（机会成本），
**与仓位大小无关**——因此减仓只砍收益、不减这部分风险。赔率约 **13:1**。

## 六、上线检查清单

放开 `enabled` / `live_order_enabled` 前逐条确认：

- [ ] **净值读取可用**。M 依赖账户净值算回撤。开发机无 QMT 时会读到 0 并
      **按安全口径永久暂停**。上线前必须确认 Windows 生产机能取到 QMT 总资产，
      或 `reports/strategy_m/m_equity_peak.json` 有有效的 `last_equity`。
- [ ] **净值峰值口径**。首次启用时 `peak_equity` 为空，会以当前净值为峰值起点。
      若账户当时正处于回撤中，M 会误判为"无回撤"。**建议在账户净值接近历史高点时启用。**
- [ ] 收盘流水线第 ⑨ 步 `run_strategy_m_signal.py` 正常产出
      `reports/strategy_m/m_signal_runs_recent.json`。
- [ ] 确认 M 在 `live_trade.exit_pov_strategy_legs` 与
      `exit_pov_large_force_strategy_legs` 中（卖出走容量型 POV）。
- [ ] 确认 `T2_CLOSE_STRATEGY_LEGS` 含 "M"（否则 M 持仓会在盘中被提前卖出，
      而不是等 T+2 收盘）。
- [ ] 已按用户决定放开`live_order_enabled=true`；先用小资金核对真实触发、成交率、
      滑点、费用和10%回撤保护，未完成前不得放大资金。

## 七、设计禁忌

> **回撤保护开关只能用于 M 这类补位腿，绝不可推广到 A/C/D/E2/L。**

M 被净值门控是安全的，因为其余五腿**无条件触发**——它们在回撤中照常交易并修复净值，
M 随后自动恢复。回测中 20240902/20240911 两次暂停后，L 的 +16.41%、+8.22% 与
E2 的 +10.56% 把净值拉回新高，M 于 20241011 恢复并赚了 +17.37%。

若把该开关推广到所有腿，**回撤中将没有任何腿能创造修复净值的机会，系统会锁死在回撤里。**

## 八、文件清单

| 文件 | 作用 |
|---|---|
| `src/strategy_m.py` | 规则唯一源，实盘与回测共用 |
| `scripts/run_strategy_m_signal.py` | 收盘流水线第 ⑨ 步，生成信号 |
| `src/combined_live_engine.py` | `build_m_buy_order_if_any()` + 兜底分支 |
| `config/config.json` → `strategy_m` | 全部开关与参数 |
| `tests/test_strategy_m.py` | 规则测试 + 两处白名单回归 |
| `reports/strategy_m/m_signals_recent.json` | 正式信号（滚动10个交易日） |
| `reports/strategy_m/m_signal_runs_recent.json` | 运行状态账本（滚动20个交易日） |
| `reports/strategy_m/m_equity_peak.json` | 净值峰值记录（回撤保护用） |

## 九、验证命令

```bash
python3 -m unittest tests.test_strategy_m
python3 scripts/run_strategy_m_signal.py --signal-date 20260803 --dry-run
```
