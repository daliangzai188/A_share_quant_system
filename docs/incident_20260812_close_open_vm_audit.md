# 2026-08-12 平仓失败、虚拟机停机与次日漏开仓综合审计

审计时间：2026-08-13  
审计范围：2026-08-10 00:00 之后的全部 Git 提交、Windows daemon/keeper 日志、
平仓执行链、开仓执行链、08:50/09:00/09:20/09:30/09:35 通知与恢复链。

## 一、最终结论

1. 8月12日 D 策略 `600815.SH 厦工股份` 未自动平仓的直接触发点是：14:55任务在
   `14:54:59.99x` 提前进入，旧墙钟门禁把“今日到期”同时判成“未到平仓窗口”。
2. 提交 `968c0df` 修复了主识别和主提交两个入口，但任务 `finally` 仍按默认墙钟复查，
   存在卖出未关账却误恢复流水线、提醒静默的残余漏洞；`fc53759` 已完整闭环。
3. 原调度器还存在两个独立风险：距离任务不足30秒时直接跳过；每项任务结束固定睡60秒，
   前项稍慢会吞掉09:23/09:26/09:30等紧邻任务。`fc53759` 已改为严格沿日程推进并在
   下单前复核北京时间。
4. 虚拟机停止不是交易程序主动执行关机。项目内不存在 `shutdown.exe`、`Stop-Computer`、
   `poweroff` 等系统关机调用；daemon和QMT账户心跳一直正常到8月12日23:28，随后Windows
   daemon、keeper、同步日志同时中断，8月13日10:40才重新启动。确切原因需读取Windows
   System事件日志（1074/41/6006/6008）。
5. 8月13日 C 策略 `600881.SH 亚泰集团` 漏开仓的直接原因是虚拟机/daemon在
   08:50、09:00、09:20、09:30全程不在线，而不是策略门禁拒绝。10:40启动时只剩候选
   播报，按回测一致性原则不能盘中追买已经过期的普通开盘信号。
6. 当前冻结策略选择和认证脚本仍逐笔一致：150笔、历史同口径复利
   `29388.980134x`、最大回撤 `-23.558528%`、胜率 `68.6667%`；本轮只修执行、恢复和
   通知，不改选股条件、腿序、持有期或收益口径，复利保留率为100%，通过70%硬底线。

## 二、8月10日之后全部提交评价

| 提交 | 主题 | 审计评价 |
|---|---|---|
| `5be387d` | 卖出成交回写、七八月验证 | 有效；提高账实一致性，不是本次漏平仓原因 |
| `9e766ee` | P0实盘认证门禁、暂停M | 有效风控；随后按用户决策恢复M |
| `b7392d0` | P1认证时效、真实成交滚动报告 | 有效；只监控，不阻断当前实盘 |
| `63dde06` | P2认证复现、策略净值账本 | 有效；M依赖账本启动时序后来继续修正 |
| `0e5d751` | 恢复M实盘并记录风险接受 | 与用户决策一致；没有关闭其他策略 |
| `cc3d351` | 忽略运行产物 | 纯仓库卫生，无交易影响 |
| `3a29a06` | M首次建账与播报时序 | 有效；避免启动误报M暂停 |
| `52db73a` | 持仓日候选让路播报 | 纯显示修正，无下单影响 |
| `1ba9d58` | D实盘/回测候选严格对齐 | 有效；D边界和成交可靠性已由测试锁定 |
| `93ba3a8` | 冻结实盘发布、锁定OOS起点 | 有效；防策略静默漂移 |
| `65081d4` | 成交容量/TCA监控 | 有效；只读监控为主 |
| `33903e3` | 事务型执行事件镜像 | 有效；不替代权威持仓账本 |
| `4029110` | 修正金额补单超额误报 | 有效；不放松85%硬顶 |
| `1aee937` | 全策略影子候选/反事实收益 | 有效；不改变实盘订单 |
| `0a47bb6` | 发布版本样本外报告 | 有效；不自动改策略 |
| `3d448ca` | OOS日评和周报提醒 | 有效；不阻断实盘 |
| `33e252f` | 完善AI复核提示 | 纯报告增强 |
| `43dd8b1` | 补全复核目标/改动说明 | 纯报告增强 |
| `6bef926` | 每周打印/推送复核提示 | 有效通知增强 |
| `d3651ca` | 随机历史区间回放 | 有效研究工具；不改实盘 |
| `986f8fb` | 成交缺口与可恢复性分类 | 有效；避免把正常持仓误报为缺口 |
| `8d075b3` | 状态快照/隔离恢复演练 | 有效；恢复默认不覆盖生产树 |
| `de7d380` | 发布复利70%硬底线 | 有效；本次执行修复保留率100% |
| `8d2cdb0` | 令牌迁移本地环境 | 有效安全修复 |
| `380244a` | 账户风险影子总闸 | 影子模式，不改变下单 |
| `1ac0a0b` | 风险总闸历史验证 | 研究验证，不改变下单 |
| `ea7a141` | 风险候选随机稳健性 | 研究验证，不改变下单 |
| `5d448d3` | 固定风险候选进入影子 | 影子模式，不改变下单 |
| `6080bdd` | 券商截图成交回补 | 有效人工证据工具 |
| `e0346d4` | 执行事件历史恢复冲突 | 有效；恢复保持版本不可变 |
| `968c0df` | 14:55调用方声明身份 | 方向正确但不完整；漏了任务结束复查和调度器根因 |
| `da39a92` | 热修后重认证/冻结 | 必要且有效；否则哈希门禁会阻断次日开仓 |
| `fc53759` | 平仓调度与尾盘复查闭环 | 本次P0修复；补齐身份、墙钟对齐、紧邻任务不漏、余仓通知 |
| `15331ff` | VM掉线告警和Windows自恢复 | 本次P1修复；Mac哨兵已实装并验证退出码0 |
| `5dc7666` | 开仓重启恢复和早盘通知 | 本次P1修复；不追过期普通信号，恢复在途单/E2/D |
| `15883c2` | 启动时自动安装Windows早盘兜底 | 本次P1修复；用户继续用原启动命令即可安装计划任务 |

## 三、修复后的执行闭环

### 平仓

`到期识别 → 14:55快速提交 → 本地未关账则保持交易优先暂停并通知 → 14:56核查连续竞价覆盖
→ 14:56:20撤单交接 → 14:57/14:59按券商真实余仓兜底 → 15:00:30双快照和系统成交账对账
→ 有余仓则通知、次日按逾期仓优先清理`。

涨停、跌停、停牌、无买盘或券商拒单仍可能导致无法成交；系统能保证的是不静默、不盲目
超卖、持续按真实余仓兜底，不能保证交易所一定成交。

### 开仓

`08:50计划通知 → 09:00组合缓存 → 09:20复核/预挂 → 09:25后禁止伪竞价单
→ 09:26成交同步 → 09:30确认/补买/POV → 09:35成交核查`。

重启恢复口径：

- 09:20前：补计划和通知；
- 09:20-09:25：补复核和竞价预挂；
- 09:25-09:30：等09:30，不在静默段盲发；
- 09:30-09:35：恢复开盘确认/补单；
- 09:35后：只确认遗留在途单，不新建过期A/M/C/L普通买单；E2按原延迟规则、D按盘中兜底规则；
- 错过窗口且无成交/在途单：主动通知。

## 四、验证证据

- 全项目单元测试：336项通过，10项因D接力已关闭而按设计跳过；0失败。
- 卖出安全专项：78项通过。
- 开仓/早盘恢复专项：7项通过。
- Windows/Mac运行恢复工具专项：4项通过。
- 实盘计划模块与认证脚本：481个信号日逐笔选择一致，150笔腿分布
  `L47/A31/M26/C17/E2 15/D14` 完全一致。
- 复利硬底线：`PASS_NONINFERIOR`，候选/锚点均为 `29388.980133715802x`，保留率100%。
- Mac VMware独立哨兵：launchd `com.asystem.vm-watchdog` 已运行两次，最后退出码0；
  旧Desktop哨兵因TCC退出码2的问题已消除。

## 五、Windows侧一次性验收

同步代码后仍使用原命令重启；`start_windows.py` 会自动检查/安装登录与每日08:15运行兜底任务。

```powershell
cd C:\A_System
py -3.11 stop_windows.py
py -3.11 start_windows.py
```

导出本次虚拟机停止的Windows证据：

```powershell
py -3.11 scripts\audit_windows_power_events.py --days 7
```

报告位置：`C:\A_System\reports\runtime\windows_power_events_latest.json`。

看到1074时查看其message中的发起进程；看到41/6008通常表示宿主强停、断电或崩溃；
看到6006且无41/6008通常是正常关机。不要把报告中的历史事件直接当成程序主动关机证据。

