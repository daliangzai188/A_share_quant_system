# 策略E：无前视R1、双因子分位评分与入场门禁V6

## 一、最终执行标准

自2026-08-24起，E只执行以下口径：

- 无前视：选股不得使用次日是否成交、退出是否成交、未来价格或未来收益；
- 单账户：上一笔E未到退出日时，不允许复用同一笔资金开下一笔；
- 实盘与回测共用 `src/strategy_e.py`，禁止研究脚本和实盘脚本各写一套筛选；
- 每日第一名在13:30~14:30首次涨停时当日空仓，禁止回补第二名；
- R1候选并集先保留`segment_retreat_state_bucket=neutral`；
- 对信号日`turnover_rate`高值优先与`amount_ratio_1d`低值优先分别计算同日候选分位，两个因子各占50%；
- 并列键固定为`scenario_rank`、`ts_code`升序，保证Mac/Windows结果一致；
- 目标仓位82.5%，累计实际成交额仍受80%验收下限和85%单票硬顶约束；
- 关键字段或成交可靠性数据缺失时拒绝生成E信号，不退回旧策略。

当前严格研究锚点为2024-06-30至2026-06-30。2024-06-30是非交易日，
首个可用信号日为2024-07-01。E严格独立单账户指标如下：

| 指标 | 数值 |
|---|---:|
| 门禁前候选信号日 | 113日 |
| V6门禁后候选池 | 89笔 |
| 单账户占仓后实际执行 | 74笔 |
| 正式A>C>E>D组合内E实际执行 | 36笔 |
| 仓位 | 82.5% |
| 独立单账户平均账户收益 | +3.8103% |
| 独立单账户中位账户收益 | +1.7922% |
| 独立单账户胜率 | 64.86% |
| 独立单账户机械复利 | 11.703790倍 |
| 独立单账户最大回撤 | -24.7461% |
| 最大单笔盈利 | +52.3193% |
| 最大单笔亏损 | -17.6291% |
| 盈亏比 | 1.8015 |
| 最大连续亏损 | 3笔 |

门禁后的89笔候选若忽略持仓时间并逐笔相乘为15.634337倍，该值仅用于候选池诊断；
它允许历史持仓重叠，不能称为E独立策略的单账户复利。正式单腿口径使用74笔、
11.703790倍。

旧50/43行、102/82日文件只保留作2024-05-20至2026-05-14历史锁定子集回归，
不再是当前比较锚点。40条R1原始规则和时间门禁仍源自历史研究；新增的成交额倍率
双因子排序也是在新锚点同窗口内搜索，160个唯一候选结果参与比较，存在多重比较、
路径依赖和过拟合风险。该规则只直接改变3个E信号日，组合增量集中于2026年6月。
当前仍是
`STRICT_DISCOVERY`、`release_eligible=false`，不得据此扩大资金或解除BUY门禁。

## 二、为什么不再使用12.028倍

旧62笔/12.028倍仅保留审计，不能作为实盘目标，原因包括：

1. 研究候选先用 `buy_executed`、`sell_executed` 过滤，信号日无法知道这些未来结果；
2. 部分交易在上一笔E仍占用资金时又开新仓，单账户无法同时使用同一笔资金；
3. 旧口径混合T+2/T+3退出，但原实盘固定T+2，规则不一致。

若把上述不可实现部分写进实盘，只会制造虚高回测，不能提高真实收益。

## 三、唯一规则源

| 文件 | 职责 |
|---|---|
| `config/strategy_e_r1_scenarios.json` | 固化40条R1场景、入场门禁、双因子最终评分、T+2/T+3退出规则和审计指标 |
| `src/strategy_e.py` | 解析规则、禁止前视字段、构造R1候选、选每日第一名并执行无回补门禁 |
| `scripts/run_strategy_e_signal.py` | 检查账户空闲状态，调用唯一规则源并生成次日实盘信号 |
| `src/combined_live_engine.py` | 把E信号转成组合计划单，并按信号退出偏移登记T+2/T+3到期日 |
| `scripts/verify_strategy_e_alignment.py` | 复核当前窗口89个双因子候选日逐票一致性 |
| `scripts/optimize_strict_acde_from_official_baseline.py` | 在新窗口内按独立腿+总组合双复利门槛搜索排序 |
| `scripts/certify_current_executable_portfolio.py` | 只核对E与当前D/A/E/C实盘选择身份，不生成正式收益 |
| `scripts/certify_strict_asof_portfolio.py` | 用严格as-of候选和统一机械复利生成当前组合唯一正式统计证书 |

## 四、选股与退出流程

```text
T日收盘完整涨停池
  → 完整数据/非ST/成交可靠性过滤
  → 40条R1规则分别筛选并取当日第一名
  → 40条结果合并，同票按scenario_rank去重
  → 只保留segment_retreat_state_bucket=neutral
  → 同日内计算turnover_rate高值分位与amount_ratio_1d低值分位
  → 两个分位各占50%，按综合分降序取第一只
  → 若第一只first_time_detail_bucket=1330_1430，当日空仓
  → 不回补同日第二只
  → T+1集合竞价与现有POV开仓
  → 按命中规则在T+2或T+3到期日退出
```

同一综合分并列时，先按`scenario_rank`升序，再以`ts_code`升序作为稳定末位键，
保证Windows实盘与Mac回放选中同一只股票。

## 五、成交与资金风控

E的候选选择和下单执行分层：

- 候选层必须通过 `allow_buy_reliable`、`is_fill_score_reliable`、`limit_data_quality=full` 和 `strategy_compatible=true`；
- 09:20集合竞价预挂和09:30至10:30 POV沿用全系统现有框架；
- 09:20开仓前总资产快照的82.5%是累计实际成交额目标；
- 累计实际成交额和最坏委托金额均不得超过总资产85%；
- 使用QMT真实可用余额并保留现金缓冲，不生成资金不足废单；
- 价格超过开盘价2%、涨停、现金不足或不足一手时停止追单；
- 有旧策略仓未实际清空时取消新开仓，不做尾盘衔接买入。

## 六、量比缺失处理

R1有规则依赖 `volume_ratio_bucket`。实盘打分表量比为空时：

1. 优先读取同日 `data/raw/daily_basic/{date}.csv` 的 `volume_ratio`；
2. 若数据源仍为空，在内存中按“当日成交量 ÷ 前5个交易日日均成交量”计算；
3. 不改写原始文件；仍无法计算时fail-closed，E当日不出信号。

其他任一R1关键字段整列为空时同样拒绝出信号，不能静默跳过部分规则。

## 七、运行与验证

正常生成信号：

```bash
python scripts/run_strategy_e_signal.py
```

指定日期只预览：

```bash
python scripts/run_strategy_e_signal.py --signal-date 20260731 --dry-run
```

逐票对齐验证：

```bash
python scripts/verify_strategy_e_alignment.py
```

当前验收结果必须同时满足：

- 40条规则；
- 当前最终排序为`turnover_rate:desc`与`amount_ratio_1d:asc`的50%/50%同日分位综合分；
- 入场门禁淘汰第一名后不回补第二名；
- 无前视选股字段；
- 新锚点E独立单账户74笔、11.703790倍，候选池89笔、15.634337倍仅作诊断，组合内E实际执行36笔；
- 当前真实开仓日严格研究组合按`A>C>E>D`为136笔、1023.791244倍、最大回撤-14.1198%；
- 研究协议仍为`STRICT_DISCOVERY`，研究证书不参与实盘BUY开关；
- 机械复利只用于同口径回归，不代表收益预期或真实容量。

当A、C或现有持仓阻断E正式信号时，E仍执行一次不下单、不写正式信号的只读候选
检查。日志和`e_signal_runs_recent.json`必须分别记录`priority_blocker`、
`candidate_check_status`、`counterfactual_e_status`及候选数：只有状态为
`CALCULATED`且候选数为0，才可说明“即使没有上游阻断，E也不会触发”；计算失败时
必须显示结果未知，禁止回退读取陈旧候选CSV。

> 当前按真实开仓日`A>C>E>D`重新回放；A/C/E使用buy_date，三腿均无计划时D才运行。
> 旧组合标尺与旧输入哈希全部失效。E的同窗口搜索风险继续如实披露。

任何一项失败，验证脚本返回失败，E不得上线。
